<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="?android:selectableItemBackground"
    android:orientation="horizontal"
    android:padding="5dip" >

    <!--  ListRow Left sized Thumbnail image -->
    <LinearLayout android:id="@+id/thumbnail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="0dip"
        android:layout_alignParentLeft="true"

        android:layout_marginRight="5dip">

        <ImageView
            android:id="@+id/list_image"
            android:background="?android:selectableItemBackground"
            android:layout_width="50dip"
            android:layout_height="50dip"
            android:src="@drawable/ic_play_black"/>

    </LinearLayout>

    <!-- Title Of Song-->
    <TextView
        android:id="@+id/song_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/thumbnail"
        android:layout_toRightOf="@+id/thumbnail"
        android:text="Skinny Berlin - Lies "
        android:textColor="#040404"
        android:typeface="sans"
        android:textSize="15dip"
        android:textStyle="bold"
        android:layout_marginTop="10dip"/>

    <!-- Artist Name -->
    <TextView
        android:id="@+id/song_artist"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/song_title"
        android:textColor="#343434"
        android:textSize="10dip"
        android:layout_marginTop="4dip"
        android:layout_toRightOf="@+id/thumbnail"
        android:text="Cypher League Media" />

    <!-- Rightend Duration -->
    <TextView
        android:id="@+id/song_duration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="right"
        android:text="4:03"
        android:layout_marginRight="16dp"
        android:textSize="10dip"
        android:textColor="#ff000000"
        android:textStyle="bold"
        android:layout_alignTop="@+id/song_title"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <!-- Rightend Arrow -->
    <ImageView android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_action_video"
        android:layout_below="@+id/song_duration"
        android:layout_alignLeft="@+id/song_duration"
        android:layout_alignStart="@+id/song_duration"
        android:layout_marginRight="9dp" />

</RelativeLayout>